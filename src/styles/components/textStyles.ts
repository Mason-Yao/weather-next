const textStyles = {
  sizes: {
    md: {
      fontSize: "15px",
      width: "100%",
      height: "100%",
      mx: "auto",
      display: "flex",
      justifyContent: "center",
      alignItems: "center"
    }
  }
}

export default textStyles