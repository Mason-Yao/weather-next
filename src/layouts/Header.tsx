import { FC, ReactNode } from "react";
import { Box } from "@chakra-ui/react";

const Header: FC = () => {
  return (
    <>
      <Box width={"100%"} height="50px">header</Box>
    </>
  );
};

export default Header;
